<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RSLink"
        tools:targetApi="31" >
        <activity
            android:name=".feature_onboarding.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.RSLink" />
        <activity
            android:name=".feature_dashboard.ui.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".feature_signin.SignInActivity"
            android:exported="false" />
        <activity
            android:name=".core.routing.RouterActivity"
            android:exported="false"
            android:label="@string/title_activity_router"
            android:theme="@style/Theme.RSLink" />
    </application>

</manifest>